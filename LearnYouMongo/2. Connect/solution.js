/*
    No code to be written for this exercise. Take the following steps:
    -----------------------------------------------------------------
    1. Create new 'data' folder in root folder: 'mkdir data'.
    2. Run 'mongod --port 27017 --dbpath=./data --nojournal' ('--nojournal' suffix is important when using Cloud 9 IDE (or command won't run)).
    3. Open new tab and 'leanyoumongo verify'.
*/